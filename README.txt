
This branch is nothing like the others.  It is meant to hold the plan templates and nothing else.

It was too late to start it from an uninitialized repository, so it's built off of the first commit
with all files deleted instead.  This branch should never be merged into any other.


